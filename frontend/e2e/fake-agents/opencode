#!/usr/bin/env bun
// A stand-in for OpenCode in browser tests: it answers every turn with ACK
// in the JSON events the real CLI prints.
console.log(
  JSON.stringify({
    type: "text",
    sessionID: "fake-opencode",
    part: { id: "part-1", text: "ACK" },
  }),
);
